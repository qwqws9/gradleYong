<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="yong.mapper.DuntokiMapper">
    <select id="selectDuntokiList" resultType="DuntokiDto" parameterType="DuntokiDto">
        select * from duntoki where main_id = #{mainId}
    </select>

</mapper>